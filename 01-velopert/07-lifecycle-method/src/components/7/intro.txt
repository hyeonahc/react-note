- 모든 리액트 컴포넌트에는 라이프사이클(수명주기)가 존재한다
- 컴포넌트의 수명은 페이지에 렌더링되기 전 준비과정에서 시작해 페이지에서 사라질때 끝이 난다
- 라이프 사이클 메소드는 클래스형 컴포넌트에서만 사용할 수 있다
- 함수형컴포넌트에서는 Hooks를 사용한다

라이프 사이클 메소드를 언제 사용할까?
- 컴포넌트를 처음으로 렌더링할때 특정작업을 하고싶을때
- 컴포넌트를 업데이트하기 전후 특정작업을 하고싶을때
- 불필요한 업데이트를 방지하고 싶을때
