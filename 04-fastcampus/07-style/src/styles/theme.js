const colors = {
  primary: '#007bff',
  success: '#28a745',
  danger: '#dc3545',
}

const common = {
  flexCenter: `
		display: flex;
		align-items: center;
		justify-content: center;
	`,
}

const theme = {
  colors,
  common,
}

export default theme
